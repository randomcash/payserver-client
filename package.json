{
  "name": "payserver-client-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Layout regression tests for styles.css. Not a dependency of the crate.",
  "scripts": {
    "test": "scripts/run-tests.sh",
    "test:ui": "scripts/run-tests.sh --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
