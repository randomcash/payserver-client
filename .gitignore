# Build output
/dist
/target

# Trunk cache
.trunk

# Written by `scripts/commons.sh link`; never commit it — it un-pins commons.
/.cargo/config.toml

# Layout tests
/node_modules
/test-results
/playwright-report

# TMPDIR for local runs, see scripts/run-tests.sh — keeps browser profiles
# and Playwright's internal temp files off a RAM-backed /tmp.
/.tmp
